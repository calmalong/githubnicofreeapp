<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hello"
        tools:targetApi="31">
        <activity
            android:name=".UpdaterecordActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EvaluationvalueActivity" />
        <activity android:name=".UserinfoActivity" />
        <activity android:name=".UserinfonextActivity" />
        <activity android:name=".PlanActivity" />
        <activity
            android:name=".ViewrecordActivity"
            android:exported="true"
            android:launchMode="singleTask" />
        <activity android:name=".DailyrecordActivity" />
        <activity android:name=".MainScreenActivity" />
        <activity android:name=".MainScreen2Activity" />
        <activity android:name=".ImageScrollActivity" />
        <activity android:name=".ChatbotActivity" />
    </application>

</manifest>